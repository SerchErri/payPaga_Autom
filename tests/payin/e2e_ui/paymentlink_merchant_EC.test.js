// ESTE ARCHIVO HA SIDO MIGRADO HACIA LA CARPETA FLOWS.
// (Mantenemos el archivo vacío con un skip porque Windows Explorer o la Terminal activa tienen el archivo bloqueado "File In Use").
describe.skip('Archivo Migrado', () => {
    test('Ignorado', () => {
        expect(true).toBe(true);
    });
});
